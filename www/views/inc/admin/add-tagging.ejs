<div class="content-page">
    <!-- Start content -->
    <div class="content">
        <div class="container-fluid">
            <br><br>
            <div class="row">
                <div class="col-md-12">
                    <div class="card m-b-20">
                        <div class="card-body">
                            <div class="form-group row">
                                <label for="example-text-input" class="col-sm-2 col-form-label">Tiêu Đề Tagging</label>
                                <div class="col-sm-10">
                                    <input class="form-control" type="text"placeholder="Nhập tiêu đề tagging" id="txtTitle" <% if (tag) { %>
                                        value="<%= tag.title && tag.title %>"
                                    <% } %>>
                                </div>
                            </div>
                            <div class="form-group row">
                                <label for="example-text-input" class="col-sm-2 col-form-label">Slug</label>
                                <div class="col-sm-10">
                                    <input class="form-control" type="text" id="txtSlug" disabled <% if (tag) { %>
                                        value="<%= tag.slug && tag.slug %>"
                                    <% } %>>
                                </div>
                            </div>
                            <div class="form-group">
                                <label>Mô Tả</label>
                                <div>
                                    <textarea required="" class="form-control" id="txtDescription" rows="5"><% if (tag) { %><%= tag.description && tag.description %><% } %></textarea>
                                </div>
                            </div>
                            <% if (tag) { %>
                                <button type="button" class="btn btn-success waves-effect waves-light" style="margin: 0 auto; display: block" id="btnUpdate" _slug="<%= tag.slug && tag.slug%>" >Cập Nhật</button>
                            <% }else{ %>
                                <button type="button" class="btn btn-success waves-effect waves-light" style="margin: 0 auto; display: block" id="btnAdd" >Thêm Mới</button>
                            <% } %>
                        </div>
                    </div>
                </div>
            </div>
        </div>
        <!-- container-fluid -->
    </div>
    <!-- content -->
    <%include ./utils/footer.ejs%>
</div>